1. Add follow options to Sniper to support NUMA:
	--num_domains', type='int', default=2, help = 'Number of NUMA domains'
	--num_cpus_per_domain', type='int', default=2, help = 'Number of CPUs per NUMA domain'
	--mem_size_per_domain', type='string', default='256MB', help = 'Memory size per NUMA domain'
	
	--numa_cache_size", type="string", default="2MB"
	--numa_cache_assoc", type="int", default=8
	--numa_cache_tags", type="string", default="LRU"
2. in dram_directory_cntlr.cc:  The message "E REQ (%lx) not handled yet because of outstanding request in the queue" should be understand
	if not handled in this file  how the message SHOULD be handled?
